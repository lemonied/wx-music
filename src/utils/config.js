export const baseUrl = 'https://chenjiyuan.shop:30002/api'

// 播放模式
export const playMode = {
  sequence: 0,
  loop: 1,
  random: 2
}

export const storageType = {
  collections: '__collections__',
  searchHistory: '__search_history__',
  recent: '__recent__'
}
