
export const currentSong = state => state.currentSong

export const currentLineNum = state => state.currentLineNum

export const currentTime = state => state.currentTime

export const lyricLines = state => state.lyricLines

export const mode = state => state.mode

export const playList = state => state.playList

export const collections = state => state.collections

export const playing = state => state.playing

export const searchHistory = state => state.searchHistory

export const recent = state => state.recent
